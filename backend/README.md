## Getting Started

First, run the development back-end server:

```bash (backend)

npm run dev

or

npx nodemon -L --env-file=.env src/app.mjs
```

Npm run dev would kill the task using the needed port.
